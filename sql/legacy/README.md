# SQL legacy

Estos scripts quedan solo como referencia histórica del desarrollo.

Para la prueba piloto no usar estos archivos. Usar únicamente:

- `../create_database.sql`
- `../reset_clean_database.sql`
